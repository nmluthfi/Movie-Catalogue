<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.example.moviewcatalogue">

    <uses-permission android:name="android.permission.INTERNET" />
    <permission android:name="com.android.example.moviewcatalogue.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.android.example.moviewcatalogue.WRITE_DATABASE" android:protectionLevel="normal"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main_menu.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main_menu.detail_item.ItemDetailActivity"
            android:parentActivityName=".ui.main_menu.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.main_menu.MainActivity" />
        </activity>

        <provider
            android:authorities="com.android.example.moviewcatalogue"
            android:name=".database.MovieTvShowProvider"
            android:exported="true"
            android:readPermission="com.dicoding.mynotesapp.READ_DATABASE"
            android:writePermission="com.dicoding.mynotesapp.WRITE_DATABASE" />

    </application>

</manifest>